namespace = cso_setup

# Autostart
event = {
	id = cso_setup.101
	hide_window = yes
    is_triggered_only = yes
	fire_only_once = yes

	immediate = {
		random_country = {
			country_event = { id = cso_setup.100 }
		}
    }
}

# Fires all the events for the custom homeworlds
country_event = {
	id = cso_setup.100
	hide_window = yes
	fire_only_once = yes
    is_triggered_only = yes

	trigger = {
		NOT = {
			has_global_flag = cso_setup_initializer
		}
	}

	immediate = {
        set_global_flag = cso_setup_initializer
        country_event = { id = cso_human.10 }
        country_event = { id = cso_advanced.10 }
        country_event = { id = cso_habitat.10 }
    }
}